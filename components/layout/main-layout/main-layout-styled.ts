import { Layout } from "antd";
import styled from "styled-components";

export const MainLayoutStyled = styled(Layout)`
  .contentWarper {
    min-height: 574px;
    background: #fff;
    padding: 76px 0;
  }
`;
/* background-color: ${({ theme }) => theme.color.backgroundGlobals}; */
